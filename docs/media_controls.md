# VuNMix media controls

Protocol v1 media actions use one byte:

- `1` — Play/Pause
- `2` — Next track
- `3` — Previous track
- `4` — Stop

The desktop media service keeps these action IDs aligned with firmware `MediaControlData`.
